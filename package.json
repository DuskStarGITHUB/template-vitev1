{
  "name": "template-vitev1",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "init": "npm install --prefix backend && npm install --prefix frontend",
    "stack": "concurrently \"npm run dev --prefix frontend\" \"npm run start:dev --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
